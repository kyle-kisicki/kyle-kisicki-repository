<!>DOCTYPE>
<html>
<head>
</head>

<body>
<?php>

// Variables? they're data types
$myvar = "I'm a string";
$othervar = 12.999
//integer
$mynum = 10;
$mynum =+ 20;
$mynum++;
++$mynum; 
-$mynum;


//variable interpolation?
echo "this will print \$myvar";
echo "this will print $myvar";
echo 'this will print $myvar';

// concatenation
$twostrings = "hi"."Kyle!";

//conditionals
$weathercondition = "sunny";

if ($weathercondition == "sunny") {
	echo"you don'tneed an umbrella";
} 
elseif ($weathercondition == "cloudy")

else {
	echo "it is not sunny"; 
}


// looping
$temp = 35;

// to make the loop end, add a counter
$mycounter = 1;

while ($temp > 32) {
	echo "it's not freezing (yet)!";
	++$mycounter;
	if ($mycounter == 10) {
	break;
	}
	echo "end of loop";
} while ($temp > 32);
	
}

//activity 2, looping

//found on internet
echo "<table border =\"1\" style='border-collapse: collapse'>";
	for ($row=1; $row <= 12; $row++) { 
		echo "<tr> \n";
		for ($col=1; $col <= 12; $col++) { 
		   $p = $col * $row;
		   echo "<td>$p</td> \n";
		   	}
	  	    echo "</tr>";
		}
		echo "</table>";


// professor's example

for (c$ = 1; $c <= 12; ++$c); {
		for ($inner = 1; $inner<+12: ++$inner) {
		echo $c * $inner; 
		}
		echo $c. "<br>"; 
}



?>




</body>
</html>